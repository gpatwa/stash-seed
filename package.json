{
  "name": "stash-seed",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "description": "Minimal runnable seed repo for a live Agentic SDLC multi-agent run.",
  "scripts": {
    "typecheck": "for f in $(find src -name '*.js'); do node --check \"$f\" || exit 1; done; echo \"typecheck ok\"",
    "test": "node --test",
    "build": "node scripts/build-check.mjs",
    "qa:mvp": "npm run typecheck && npm run test",
    "start": "node src/server.js"
  }
}
